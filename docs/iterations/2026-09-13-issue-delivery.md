# Pairlet 本期任务管理 · 2026-09-13

更新时间：2026-09-14。适用范围：本轮 Issue 分派、修复、审查与交付收口。目标发布版本及截止日期尚未指定。

本文用于安排本期工作、记录剩余事项和验收结果。[GitHub Issues](https://github.com/heypandax/pairlet/issues) 保留需求正文与讨论；本文复用原 Issue 编号，不另建需求编号。状态是核验日期的快照，后续推进应同步更新本文及对应 Issue 的交付证据。清单中的待办和建议不自动触发提交、部署或发布。

> **交付收口 · 2026-09-14**：用户已授权提交并推送 `main`。保留前两批 5 个提交，第三／四批按 6 组代码提交收口，本页作为第 7 组维护文档随代码交付；准确映射见第 6 节。功能开发继续暂停，后续任务不自动启动。此页所属提交是否已进入远端，以 `origin/main` 的包含关系和推送回执核验；Git 合入不等于版本发布。
>
> 阶段恢复资料已另存本机持久目录；当前主工作区的 `_local/issue-closeout-2026-09-14/README.md` 是本地入口。包含候选文件及 SHA-256、原有提交的增量 Git bundle、验证／复审／模型回执和续接说明。构建缓存及 SDK 可重新生成，原工作区和临时原件均保留。归档状态与校验清单以本地入口为准。

> **提交与设备记录**：6 组代码提交已创建，逐组 tree 与预先回放的分组一致，58 个业务／测试／构建文件与已验证候选逐字节一致。桌面端、Pandaa iPhone、iPad 模拟器及 daemon 的开发同步均已完成，详见第 2 节；实际业务与硬件验收的缺口仍保留。

## 1. 当前概况

**前三批已有代码成果：11 个 GitHub Issue，加 1 项未单独建单的 Codex exit 143 修复。第四批 #362、#360 阶段一、#334 适配范围已完成编码、独立复审与本地组合验证，详见第 5 节。本期交付目标为 `main`，代码提交共 11 个；正式版本尚未发布。**

| 分组 | 数量 | 当前状态 |
|---|---:|---|
| 前两批已提交代码的 Issue | 8 | 本轮修复代码及审查已完成；部分 Issue 仍有能力或现场验收限制 |
| 第三批已提交修复的 Issue | 3 | 13 个业务文件及相应兼容清单已按 Issue 提交；仍有部分未解决需求 |
| 第四批与其余独立 Issue | 7 | 第四批三项候选通过本地验证；仍保留原 Issue 的后续范围和现场验收 |
| Linux 桌面打包 | 1 | 仅前期调研，无实现代码；Linux 验证已停止 |
| 反馈总览 | 1 | 只汇总跟踪，不单独开发 |
| 启动时 GitHub open 合计 | **20** | 2026-09-13 启动核验无 open PR；open 不代表完全没做过 |

额外的 Codex exit 143 修复未计入上述 20 个 Issue。本文不以“已处理数／open 数”计算完成率，因为部分需求只完成了一个明确子范围。

### 状态口径

| 状态 | 含义 |
|---|---|
| 本轮代码已提交 | 已有本地 Git 提交，不能据此认为用户已获得修复 |
| 候选待提交（历史阶段） | 有实际 diff、验证和审查结果，尚无对应提交；本期第三／四批已越过此节点 |
| 部分解决 | 某个已证实缺口已修，但 Issue 的其他症状、能力或现场证据仍未闭环 |
| 待继续／待取证 | 尚未开始后续实现，或必须先取得能支持判断的证据 |
| 停止验证 | 不再投入指定验证活动；不等于永久取消平台支持 |
| 已发布／可关单 | 修复进入实际发布版本，且该 Issue 要求的交付范围有对应证据 |

## 2. 本期收口清单

以下保留恢复工作后的建议顺序；本阶段已暂停，未完成节点不自动执行。

- [x] 完成前两批代码、本地验证与复审，并本地提交。
- [x] 整合第三批 #364、#363、#320 的 13 个业务文件，核对与审查快照一致。
- [x] 完成第三批完整 daemon／desktop 回归，以及 iOS／Android 编译。
- [x] 完成 #320 非作者复审、#363 权限专项审查和 #364 协调者代码审查。
- [x] 按 2026-09-13 用户决定停止 Linux 验证，并关闭为此启动的 Docker。
- [x] **提交第三／四批代码**：按 #364、#363、#320、#360、#334、#362 共 6 组提交；共享文件按依赖分两次暂存，逐组内容及 whitespace 检查通过。本文与文档首页另作一组，不计入 58 个业务／测试／构建文件。
- [x] 完成第四批 #362、#360 阶段一、#334 的编码、非作者复审、组合回归与移动端编译。
- [x] **同步开发设备**：2026-09-14 桌面端构建安装重启成功，安装 JAR 一致且界面可见实时会话；Pandaa iPhone 真机构建、安装及拉起成功；iPad Pro 11 M4／iOS 26.2 模拟器最新构建安装拉起成功，双栏界面可见 MacBook-Bro 在线。daemon 脱离更新后回读为单实例 PID 55050、8799 正常监听、relay attached 且心跳正常，70 个安装 JAR 与本批 installDist 一致。安装启动不代替项目置顶、系统多任务或外设验收。
- [ ] **补对应的实际使用验收**：聚焦本期有修改的平台和路径；Windows 原现场、手机权限复现、真实 provider 仍分别记录，不自动追加 Linux 验证。
- [x] **形成 main 交付记录**：按用户要求直接推送 `main`，保留已有历史；准确代码提交映射见第 6 节，推送前全量合并门禁通过。未创建 PR；远端结果由本次推送及分支回读记录证明。
- [ ] **确认发布版本与覆盖平台，再发布**：回读实际发布资产和更新结果，不能用本地构建代替。
- [ ] **逐项回写与关单**：只关闭满足范围的 Issue；#320、#363 等残余事项未闭环时继续保持 open。

本机 daemon 操作遵循 [AGENTS.md](../../AGENTS.md)。本会话由 daemon 驱动，更新使用 `scripts/update-local-daemon-detached.sh`；保留其他活跃会话确认门，不能绕过它，也不能另起第二个 daemon。本次更新后已回读安装文件、单实例与连接状态；桌面端及 iPad 模拟器已观察到在线界面，Pandaa 已获安装与启动回执。进一步实际业务验收仍按各 Issue 的缺口开展。

## 3. 已本地提交：前两批

以下“已处理”指本轮代码范围完成，不表示整条 Issue 已完成真实设备或发布验收。

| Issue／问题 | 本轮已处理内容 | 本地提交 | 剩余工作与验收关注 |
|---|---|---|---|
| [#371 默认平铺](https://github.com/heypandax/pairlet/issues/371) | 未保存偏好时默认平铺；保留明确保存的树形／平铺选择 | `2360d063` | 新安装默认值及已有偏好保留的实际 App 验收；随版本交付 |
| [#377 DSH／Kimi 图片](https://github.com/heypandax/pairlet/issues/377) | 按握手能力发送 ACP 图片；覆盖纯图片、排队与不支持时的明确拒绝；修复 Kimi 首发缓冲覆盖 | `d5c4fd40` | 真实 provider 收图、排队和拒绝后继续发送的验收 |
| [#376 DSH 会话与历史](https://github.com/heypandax/pairlet/issues/376) | V0–V3 会话发现及 raw／zstd 回放；代际切换、游标与错误提示；未知格式不覆盖已有历史 | `fdd64a55`、`d3f92c95` | 真实 DSH 历史验收；**Web 分组仍受上游能力限制，仅说明限制及可行路径，未伪造分组支持** |
| [#354 ZCode／DSH 文件能力](https://github.com/heypandax/pairlet/issues/354) | 依据可信变更证据列出／读取文件；统一安全路径身份；修复 macOS 固定别名；缺少完整 diff 时明确降级 | `d3f92c95` | 真实变更文件打开／读取；确认普通 symlink 替换仍被拒绝；缺少证据时不伪造 diff |
| [#373 最近项目收起与定位](https://github.com/heypandax/pairlet/issues/373) | 默认展示 5 个项目；展开／收起；运行中项目选择后展开与定位 | `d3f92c95` | 实际侧栏交互、跨电脑目标与刷新后的稳定性 |
| [#374 首个会话漏显](https://github.com/heypandax/pairlet/issues/374) | 修复最近区域会话身份／目录匹配与显示问题 | `d3f92c95` | 最新项目首个会话无须手动刷新即可出现；结合慢回包场景观察 |
| [#375 CI 时序竞争](https://github.com/heypandax/pairlet/issues/375) | 修复 HandoffTerminalSinkCutTest 过期扫描偶发多收一帧的测试时序 | `d3f92c95` | 后续已安排的 CI 运行中观察稳定性；不为此单独追加 Linux 验证环境 |
| [#378 手机横屏单栏](https://github.com/heypandax/pairlet/issues/378) | 手机横屏进入会话默认全宽单栏，保留平板布局策略 | `d3f92c95` | 手机横竖屏切换、返回列表和现有平板行为的实际 App 验收 |
| 未单独建单：Codex exit 143／ANSI | 仅将已完成回合且主动进入 TURN_BOUNDARY 的 TERM 退出识别为正常收尾；继续处理队列；错误展示清理 ANSI、保留原始 stderr | `c8c09491` | 普通停止、异常 143、强杀仍保持原语义；**本次不证明 MCP 请求失败或 DNS 故障已经解决** |

前两批修改者按已执行分派保留真实记录；本次集成历史保留上述 5 个提交，并追加第三／四批提交。

## 4. 已提交范围：第三批

三项修改者均为 **Astra `gpt-6-astra`**，实际运行模型记录已核验。修改者和审查者的职责分别记录。

| Issue | 当前结果 | 尚未解决／待取证 | 下一动作与完成条件 |
|---|---|---|---|
| [#364 Windows 更新命令](https://github.com/heypandax/pairlet/issues/364) | 修复可复现的跨系统命令生成缺陷；系统与包目录双重判断，managed 安装优先，未知来源走主机对应安装脚本 | 原 Windows 机器的安装路径、版本及所见 App／daemon 更新区块尚未确认 | 代码已提交 `06524dcd8157`、本机 daemon 已更新；在目标机器核对 `status` 中的更新命令与实际安装方式。不能只凭本机平台矩阵把原反馈宣布归因完成 |
| [#363 新建默认模式／模型](https://github.com/heypandax/pairlet/issues/363) | **部分解决**：新建时同步固定模型及后端，排队和失败重试复用同一选择；保留 null、显式模型及恢复会话语义 | **手机“全自动变每步询问”仍未归因**；effort 等其他字段不在本次快照范围内 | 模型时序修复已提交 `1a4b992ca31a`；再次出现权限问题时取创建端版本、当时默认设置与对应 daemon open 日志，区分设置、面板和传输／daemon 消费。继续保持 open |
| [#320 模型与上下文状态](https://github.com/heypandax/pairlet/issues/320) | **部分解决**：OpenCode 从最新可信请求恢复用量，包含 cache.write；缺必要缓存字段保持未知；ZCode 使用最后一次请求的输出，避免混入整轮累计输出 | DSH／ZCode 历史占用仍缺可靠输入；部分后端窗口容量未知；未知状态的说明和真实用户表现尚未完整闭环 | 已证实修复已提交 `07e3c672537c`；取得 provider 的可靠能力／转录证据后再补剩余部分。验收新建、恢复、继续一轮及重连，不能猜测窗口或累加工具调用冒充上下文 |

### 本轮验收记录

以下为第三批当时的验证记录，后续整批验证见第 5 节、设备同步见第 2 节；不是当前文档编辑重新跑了一遍测试。

| 验证范围 | 结果 | 解释与边界 |
|---|---|---|
| daemon 完整回归 | 1976 通过，2 跳过，0 失败／错误 | 跳过 Windows 命令转发及需显式开启的真实 DSH 调用用例 |
| desktop 完整回归 | 1505 通过，0 跳过，0 失败／错误 | 包含新增默认模型／后端快照回归 |
| 合计 | **3481 通过，2 跳过，0 失败／错误** | 聚焦／独立复跑不再重复计入总数 |
| iOS／Android 编译 | iOS arm64 Kotlin、Android debug Kotlin 编译通过 | Android 使用仓库自带 Firebase 占位配置；未安装到手机 |
| daemon 分发构建 | installDist 成功 | 当时仅构建，后续第三／四批已一起安装，见第 2 节 |
| #364 聚焦与审查 | 25 项通过；协调者审查无新增发现 | 修前跨系统命令回归失败；未复现原 Windows 现场 |
| #363 聚焦与专项审查 | 82 项通过；非作者权限审查无可证实安全发现 | 新回归修前 6 失败、2 对照通过；不能由此证明原权限问题已修 |
| #320 聚焦与复审 | 作者最终 121 项通过；独立复跑 84 项通过 | 非作者发现“缺失缓存字段被当零”后已修正并复审；实际文件哈希一致 |

## 5. 待继续的任务与后续候选

2026-09-13 用户已批准启动第四批编码：#362 项目置顶同步、#360 侧栏稳定性、#334 iPad 多任务。三项均已交给 Opus 5 正式实现；#352 因当前没有可用的 iPad＋妙控键盘保持待复现，未修改其输入代码。

| Issue | 已有基础／当前状态 | 下一项具体工作 | 依赖／验收条件 |
|---|---|---|---|
| [#362 置顶跨端同步](https://github.com/heypandax/pairlet/issues/362) | 按电脑隔离的项目置顶、持久化离线操作、owner 同步及提示已实现；wire／安全与功能返修均复审通过 | 本机 daemon 已更新；安排真实双端同步验收 | 同一电脑两端一致，多电脑互不串数据，离线合并不丢改动，旧客户端兼容；会话置顶不在本批 |
| [#360 分组／排序被刷新打乱](https://github.com/heypandax/pairlet/issues/360) | 阶段一候选已独立审查并整合：非当前项目保留分组快照与空组，快照操作只读 | 阶段一已提交 `51072d7df46c`；显式导入另拆后续阶段 | 聚焦 116 项通过，独立新增 12 项通过；原反馈中外部扫描的因果关系仍未证明 |
| [#352 iPad 妙控键盘](https://github.com/heypandax/pairlet/issues/352) | v1.9.7 历史反馈：触控板点击可用、滚动异常；另有中文候选栏定位问题，当前版本待复现 | 在当前 App 版本复现两种问题，分别定位 | iPad＋妙控键盘；触控板和手指滚动均正常，候选栏跟随输入位置 |
| [#334 iPad 二期](https://github.com/heypandax/pairlet/issues/334) | 5 文件候选已独立审查并整合；全屏限制移除，跨阈值保留状态与输入焦点 | 本地验证完成；补真实多任务窗口及键盘验收，商店截图留待版本确定 | 窗口尺寸变化和输入体验验证；真实外设与商店素材分别验收 |
| [#347 ChatGPT 接管体验](https://github.com/heypandax/pairlet/issues/347) | 已有原生名称标识“CC Pocket · 原名” | 验证无需重启即可出现；研究直接在电脑 ChatGPT 中打开 | 核对真实第三方 App 行为及受支持入口，不能靠重复命名修复冒充完成 |
| [#342 海外用户激活](https://github.com/heypandax/pairlet/issues/342) | 已有信任说明、安装方式与引导／演示观测基础 | 读取后续信号，选择要验证的激活改进 | 以真实观察评估效果；不在公开文档记录用户行为明细或内部比例 |
| [#367 跨设备 Agent CLI](https://github.com/heypandax/pairlet/issues/367) | 未开始实现，属于较大功能 | 先定义最小跨设备调用流程及权限／结果契约，再拆实施阶段 | 明确目标电脑与会话、授权、超时／取消、结果返回、循环调用和资源限制；复用既有安全边界 |

### 第四批执行：3 项范围完成本地验证＋1 项待复现

**新分工：提高 Opus 5 的实现占比。复杂任务优先由 Astra `gpt-6-astra` 诊断、冻结方案，由 Opus 5 `claude-opus-5` 完整实现；常规与长消耗任务继续交给 Opus 5，独立的小修仍可由 Astra 直接完成。** 本批下面四项的预定实现者均为 Opus 5，Astra 聚焦方案及独立审查。该规则只影响后续分派，不改写前三批的实际作者记录，也不设置 token／耗时比例。

以下范围已获得编码授权；优先级是本次排期建议，未写回 GitHub 标签。Astra 已冻结 #362 同步契约及 #360 显示层根因修复。三项首轮及 #334 焦点返修的完整回执均已核实为 `claude-opus-5`；#362 评审返修的正式成功回执也已核验。原回执校验器误拒多轮后台续跑输出的问题已修复，经 14 项回归与独立审查；历史 runner 退出码保留，模型核验与产品验收分别判断。

启动核验发现 #360 可以只修改 `DesktopModel.kt`／`RepoDesktopModel.kt`／`Sidebar.kt`，#362 保留现有 `pinnedPaths`／`togglePin` 接口，因此原先的共享文件串行依赖已解除。三项在不同工作区并行实现。

| 次序／Issue | 用户收益与排期理由 | 分类／规模／风险 | 方案／诊断 | 实现分派 | 范围与验收摘要 | 当前执行状态 |
|---|---|---|---|---|---|---|
| 1 · #362 · P2 | 两端共用常用项目，减少重复整理；同步能力明确缺失 | 困难／L／高：离线冲突、持久化迁移、wire 兼容 | Astra | Opus 5 | 项目置顶、取消和顺序按电脑同步；离线改动重连后按契约收敛 | wire／安全与功能复审通过，99 项最终聚焦及组合验证通过，已提交 `859bcaf49acf` |
| 2 · #360 · P2 | 保住侧栏整理结果，避免切项目后丢失分组显示 | 常规／M／中：根因已收敛至桌面分组快照与显示；完整 Issue 仍需后续设计 | Astra | Opus 5 | 修复已证实的显示缺口，保留会话运行状态更新 | 4 文件候选已整合，聚焦及独立回归通过 |
| 3 · #334 · P2 | 让已有 iPad 双栏在多任务窗口内可用；启动时全屏配置有明确限制 | 常规／M／中：iOS 配置、尺寸变化和输入适配 | Astra 冻结 brief | Opus 5 | 解除全屏限制所需适配；跨宽窄阈值保留会话界面状态 | 焦点返修完成；最终 17 项回归与模拟器重建通过，已提交 `f55297306312` |
| 条件候选 · #352 · P2 | 修复用户已反馈的触控板滚动和中文候选栏问题 | 困难／暂估 M／中；当前设备证据缺失 | Astra | Opus 5 | 两个症状分别复现、定位、修复并回归触控输入 | 当前版本的 iPad＋妙控键盘复现；只有仍存在的症状进入编码 |

#### 第四批验收记录

| Issue | 已有证据 | 当前剩余事项 |
|---|---|---|
| #360 | 修前真实侧栏 12 项中 11 失败、1 对照通过；修后 8 套件 116 项通过，Astra 独立新 12 项通过。额外 2 项去守卫反证在精准断言失败，源码恢复后哈希与通过版本一致。正式 Opus 5 回执通过 | 4 文件已整合，组合回归通过；原 Issue 显式导入与实际用户场景另行验收 |
| #334 | 最终独立回归 17 项通过（7 项连续性、8 项手机、2 项宽布局）；真实 Compose 组件的 Focused 语义、选区、草稿、滚动及聊天内问题卡跨 1024→699→700→320→1024 保留。最终 Xcode iPad Simulator Debug 重建、安装、启动与旋转通过；使用公开 Firebase 占位配置 | 5 文件已整合，组合回归通过。真实系统窗口缩放／Split View／Slide Over、原生软键盘持续弹出、输入法组合态与外接键盘未验收；新增用例未覆盖根层 SecureApprovalSheet |
| #362 | 项目置顶同步已完成 daemon／wire、客户端持久化、离线队列、连接隔离与最小提示接线；独立 wire／安全及功能复审通过。最终客户端七套聚焦 99 项通过；原拒绝操作、跨窗口隐藏、有效映射和实际存储恢复均有回归证据 | 本地组合回归已通过；真实双端同步、重新配对与物理断电恢复尚未验收。会话置顶同步及手机新入口不在本批 |

以上聚焦统计绑定对应候选，不与下表重复相加；隔离测试或模拟器启动不代表真实多设备同步、物理断电或外设验收。

**最终组合回归（2026-09-14）**：`bash scripts/check-all.sh --affected` 通过，protocol 改动触发六模块检查。表内计数来自最终候选的实际 XML，聚焦重复运行不累计。

| 模块 | 通过 | 跳过 | 本轮执行情况 |
|---|---:|---:|---|
| protocol | 294 | 0 | 实际执行 |
| daemon | 2034 | 2 | 实际执行；跳过 Windows cmd 现场转发和需显式启用的真实 DSH 调用 |
| mobile desktop | 1612 | 0 | 实际执行；包括 #363 的 8 项、#360 的 12 项及 #334 的连续性回归 |
| relay | 73 | 0 | 实际执行 |
| observability | 27 | 0 | Gradle 复用未变输入的缓存结果 |
| observability-sentry | 18 | 0 | Gradle 复用未变输入的缓存结果 |
| **合计** | **4058** | **2** | **4013 项本轮执行通过，45 项复用缓存；0 失败／错误** |

完整脚本首次在 Gradle 前发现品牌兼容清单过期；核对 #364 更新命令与 #362 默认关闭的新增能力字段后，仅更新对应两条哈希及说明，保留原兼容标识与全部检查。随后完整脚本通过，测试复用刚完成的同一候选结果。最终 `:mobile:composeApp:compileKotlinIosArm64` 与共享 Android `compileAndroidMain` 实际编译通过；`:mobile:androidApp:compileDebugKotlin` 检查通过但该入口为 NO-SOURCE。Android 仅临时使用仓库公开 Firebase 占位配置，执行后已移除本次临时副本；未装到真实手机。

本次新增业务与测试代码均由精确 `claude-opus-5` 实现，Astra 负责方案和非作者审查；运行回执与最终代码哈希均已核对。执行工具额外修正了本地多轮 Opus 回执校验（14 项回归），同一 runner 增加可选 effort 覆盖；本批后续运行采用 high，模型保持 Opus 5，历史失败回执保留。该工具修正不在仓库业务 diff 内。

#### #362：项目置顶同步

- **启动时的代码事实**：`PocketRepository.kt` 通过本地 `pinned_projects` 保存路径，桌面项目置顶复用这份列表；桌面会话置顶另存 `desktop_pins`。项目清单当前未按目标电脑分区，历史本地数据不能直接复制到所有配对电脑。
- **本批范围**：只同步项目置顶，保持现有置顶入口。以目标电脑为归属，复用既有加密连接，由 daemon 持久化共享状态；客户端保留离线操作。会话置顶与手机新入口留在原 Issue 后续范围。
- **已冻结的契约**：以配对电脑身份隔离；daemon 规范路径、按提交到达顺序处理同一项目冲突；不同项目离线改动合并。持久化操作序列与去重游标后才确认，旧回复不能覆盖新状态。重配对或存储重建时，未确认旧操作保留为可恢复的本地状态，不能自动重放。旧端能力默认关闭，模糊归属的历史置顶不跨电脑广播。
- **Opus 5 文件域**：`protocol` 的消息／兼容用例、daemon 的置顶持久化／路由与必要传输接线、`PocketRepository.kt`、新的平台专用置顶持久化辅助类／测试、置顶专属发送队列隔离及最小错误提示接线；保持现有项目置顶公共接口，不修改 #360 的三个桌面文件。独立审查覆盖撤销配对后的发送、订阅代际与真正的原子持久化。
- **完成条件**：桌面→手机与手机→桌面置顶／取消／顺序一致；两端离线新增不同项目后均保留；同项目并发操作按已定规则收敛；旧快照不会让已取消项无故复活；重启、重连、切电脑不串数据；新旧端组合保持可用。安排 wire 兼容专项审查，并核对 owner／guest 权限边界。

#### #360：先处理刷新稳定性，再规划显式导入

- **启动时的代码事实**：`SessionGroups.kt` 已按项目持久化分组与成员关系，`RepoDesktopModel.kt` 根据访问记录、当前目录和快照派生侧栏；`PocketRepository.kt` 会应用被接纳的 `Sessions` 回包。尚不能据“刷新后消失”推断磁盘分组已被删除，外部扫描是原反馈中的推测。
- **阶段一（本批）**：已证明 `Visit` 只保存会话，非当前项目的 `recentRows` 不使用分组，切项目后退回平铺及会话时间顺序，空分组也消失。Opus 5 补上按电脑／规范目录区分的分组快照，保留命名分组的顺序与空组，组内会话仍按活动时间排序；非当前项目的快照只读，避免现有修改接口误改当前项目。不能由这个缺口推断磁盘数据曾被删除。
- **实际文件域**：`DesktopModel.kt`、`RepoDesktopModel.kt`、`Sidebar.kt` 与桌面回归。与 #362 的写入范围独立，可以并行；daemon 仍是分组的权威来源，不新增另一份客户端持久化分组数据。
- **完成条件**：可证实的失败场景修前失败、修后通过；已有命名分组顺序与成员关系在刷新后保留；真实 App 重启持久性另行验收，组内活动时间排序保持现有行为；运行状态仍及时变化；#373 最近项目定位、#374 首个会话显示不回退。模拟回包的测试结果与实际侧栏使用分别记录。
- **阶段二（本批不编码）**：原 Issue 要求的“搜索并显式导入”继续保留，需另定已管理会话与可发现会话的边界、既有会话迁移和导入交互。仅修阶段一不满足整条 Issue 的关单条件；增加导入界面时再进入相应 UI 设计流程。

#### #334：iPad 多任务适配

- **启动时的代码事实**：`iosApp/iosApp/Info.plist` 仍为 `UIRequiresFullScreen=true`；`iosApp/project.yml` 保留第一阶段的全屏约定；现有共享布局在 700dp 附近切换单／双栏。不能把改一个开关当作完整适配。
- **本批已实现范围**：Opus 5 移除全屏限制，保持通用设备族与既有横竖屏配置；`WideLayout.kt` 使用稳定聊天父容器，在切换单／双栏时自然保留状态、焦点与选区。没有新增导航或主动恢复焦点。
- **实际文件域**：`iosApp/iosApp/Info.plist`、`iosApp/project.yml`、共享 `ui/WideLayout.kt` 和两份布局测试；`.xcodeproj` 原配置已正确，未修改。
- **完成条件**：目标系统上可进入支持的 Split View／Slide Over 模式；窗口跨单／双栏阈值时，选中会话、消息草稿、滚动和审批面板状态保留；软键盘／外接键盘、旋转及已有手机横屏行为无回退。模拟器能覆盖的窗口行为与真实外设验收分别记录。
- **后续交付**：iPad 商店截图继续属于 #334，待布局验收和目标版本明确后制作；准备素材／提审时遵守 [App Store 检查](../APP-STORE-REJECTIONS.md)。本次准备不包含商店上传或提审。

#### #352：当前版本复现后再投入实现

原反馈来自 v1.9.7，而当前仓库已升级到 Compose Multiplatform 1.12.0，不能假定旧问题仍然存在，也不能认定升级已经修好。Astra 分别检查列表／会话页的双指滚动和中文候选栏定位；注意 `MainViewController.kt` 中 `OnFocusBehavior.DoNothing` 是已有键盘避让策略的一部分。Opus 5 只修改已定位的输入／互操作路径，回归手指滚动、触控板点击、焦点和输入框避让。没有对应外设时保持待取证；不重复升级依赖或添加猜测性的全局手势补丁。

#### 基线与串并行安排

1. 第四批以 `d3f92c95` 加第三批 13 个已核验业务文件为基线；已逐文件核对三个新工作区的初始哈希。第三批源工作区保留，未为建立基线提交旧改动。
2. #362 的同步契约与 #360 的显示层修复均已由 Astra 冻结，分别交给 Opus 5。#362 不改桌面模型接口，#360 不改 `PocketRepository.kt`，以明确文件所有权解除原先的串行依赖。
3. #334 可在独立工作区由另一条 Opus 5 任务实施。#352 的设备复现可独立安排，代码修改则与 #334 的宿主／输入文件协调；无外设证据不占用实现任务。
4. 每项实现由 Opus 5 负责配套回归和审查返修，协调者核验正式运行的精确模型回执。Astra 审查实现及验收证据；触及 wire、安全或权限时增加适用的非作者专项审查。
5. 本批范围聚焦桌面／手机项目管理与 iPad 体验；#367 跨设备 CLI 另排方案阶段，#347 等第三方能力证据、#342 等激活信号。#379 按下文继续停止 Linux 验证。

### #379 Linux 桌面打包

- 需求：[deb／rpm／AppImage，amd64／arm64](https://github.com/heypandax/pairlet/issues/379)。
- 实际状态：启动过 Opus 5 前期调研；工作区无实现代码，没有完整执行完成回执，也没有构建出的 Linux 桌面安装包。
- **2026-09-13 用户决定：停止 Linux 验证。** 原因是 Linux 用户反馈很少，实际是否有人使用尚不明确，不能表述为“没有 Linux 用户”。
- 本期不主动追加 Linux 容器、实机、多架构构建或安装验收，不为其启动 Docker 或准备新环境；有明确需求、使用证据或新的用户要求后再评估投入。
- 该决定不代表删除已有 Linux 支持。#379 保持未完成；是否继续其实现另行排期，不能把调研或停止验证计作交付。

### #357 反馈总览

[#357](https://github.com/heypandax/pairlet/issues/357) 只汇总各类用户反馈。进展由对应独立 Issue 承接，不为总览重复开发或重复计算交付数量。

## 6. 工作区、提交与证据维护

### 当前工作区

所有路径以仓库同级的 `cc-pocket-worktrees/` 为起点。

| 工作区／分支 | 状态与用途 |
|---|---|
| 主仓库 `main` | 本次交付目标；推送前核验远端基线为 `4c8e9284d0be97769628aabfe8dd0d943b4e8e68`，无需处理其他新增提交；本轮纳入下方 11 个代码提交及本页 |
| `issue-batch-0913`／`fix/issue-batch-0913` | 第一批，本地 HEAD `fdd64a55`，工作区干净 |
| `issue-batch2-0913`／`fix/issue-batch2-0913` | 第二批，本地 HEAD `d3f92c9552c4d8c4ea26e75f301d11627e33fa73`，工作区干净 |
| `issue-batch3-0913`／`fix/issue-batch3-0913` | 第三批保留工作区；基线 `d3f92c95`，含第三批 13 个未提交业务文件及启动第四批前的任务文档 |
| `issue-379-batch3`／`fix/issue-379-linux-desktop` | 同一基线，无实现代码；Linux 验证停止 |
| `issue-batch4-0913`／`fix/issue-batch4-0913` | **第四批集成工作区，本文当前所在位置**；从 `d3f92c95` 加第三批 13 个已审文件及任务文档的精确副本开始，原第三批保留；#362 在此实现 |
| `issue-334-batch4`／`fix/issue-334-ipad-multitasking` | 同一基线与第三批文件副本；Opus 5 独占 iOS 配置与宽窄布局状态保留范围 |
| `issue-360-batch4`／`fix/issue-360-sidebar-groups` | 同一基线与第三批文件副本；Opus 5 独占桌面分组快照与显示范围 |
| `issue-362-client-state-batch4`／`fix/issue-362-client-state-batch4` | #362 客户端状态与持久化返修的隔离工作区；复制已冻结候选与共享 wire，由 Opus 5 实现，完成后只整合其负责的文件 |

前两批保留原提交顺序：`2360d063` → `d5c4fd40` → `c8c09491` → `fdd64a55` → `d3f92c95`。后续 6 个代码提交如下，本页再作独立维护文档提交；提交链接仅用于追溯代码，不表示版本已发布。

| 次序 | Issue／范围 | 实际代码提交 |
|---|---|---|
| 1 | #364 | `06524dcd81571ef0b140f734db4158e85dbbfbac` |
| 2 | #363 | `1a4b992ca31aebeef432c567850864e6bad363e2` |
| 3 | #320 | `07e3c672537c900d04861aba5284fa33de28294a` |
| 4 | #360 | `51072d7df46c76f6eb0ba4f65e172744cc6c8368` |
| 5 | #334 | `f5529730631225f37f17aa1918ba6dd6c9c26ad1` |
| 6 | #362 | `859bcaf49acf510787404d5c6c5d874282a0d663` |

提交前再次运行无参数 `bash scripts/check-all.sh` 全量合并门禁，结果通过（42 个 Gradle 任务，6 个执行、36 个 up-to-date；测试复用同一候选已有结果，不另累加通过数）。逐组提交均运行 repository content 与暂存 whitespace 检查；6 组 tree 精确匹配已核验分组。

### 更新本文的方法

1. 按原 Issue 编号更新对应行，分别记录本轮完成范围、仍未解决的部分和下一动作。
2. 提交后补准确 SHA；PR 创建／合并后补真实链接；发布后补版本与实际覆盖平台。
3. 测试结果绑定对应代码状态。源码变化后重新判断哪些检查需要重跑；已通过的本地测试不自动升级为设备或线上验收。
4. 原始日志、模型回执和运行资料保留在仓库外或已忽略的本地目录；本文保留必要摘要。后续交付时把可复核摘要放入对应 PR／Issue，不依赖临时文件作为长期唯一证据。
5. 一条 Issue 满足其范围后才记录可关单；部分修复继续列出缺口。新版本使用者的反馈与本机环境证据分开记录。
6. 文档维护不顺带执行 Git、部署或外部回写；对应动作按当前用户授权和仓库规则推进。

## 7. 本期变更记录

| 日期 | 记录 |
|---|---|
| 2026-09-13 | 前两批本地提交完成，最新提交 `d3f92c95` |
| 2026-09-13 | 第三批 #364／#363／#320 候选整合；13 个业务文件与审查快照一致，完整回归及移动端编译通过 |
| 2026-09-13 | 按用户要求停止 Linux 验证，#379 尚无实现代码；本机第三批 daemon 更新尚未执行 |
| 2026-09-13 | 创建本期任务管理文档；GitHub 核验为 20 个 open Issue、无 open PR |
| 2026-09-13 | 按用户新分工提高 Opus 5 实现权重，复杂任务采用 Astra 方案→Opus 5 实现；准备第四批 #362／#360 阶段一／#334 与条件候选 #352，尚未启动实现 |
| 2026-09-13 | 用户批准启动第四批；三项正式 Opus 5 实现已开始，原第三批文件保留；#360 窄化文件域后可与 #362 并行，#352 缺对应硬件继续待复现 |
| 2026-09-13 | 第四批 #360 阶段一与 #334 焦点返修完成，分别整合 4／5 个文件；#362 首版完成后进入同模型评审返修，整批验证待完成 |
| 2026-09-14 | 第四批三项实现及独立复审完成；#362 最终 99 项聚焦通过。组合验证发现兼容清单未同步 #364／#362 的已审文件哈希，核对兼容标识后只更新对应清单项；组合检查已通过，最终移动端编译通过；候选待提交，本机更新另记执行结果 |
| 2026-09-14 | 本机 daemon 安全更新完成；回读单实例、relay attached、心跳及 70 个安装 JAR 一致性通过。第三／四批代码仍未提交、推送或发布 |
| 2026-09-14 | 按用户要求阶段暂停；冻结 60 文件候选，另存代码恢复快照、5 个已有本地提交与验证证据；保留全部源工作区，未启动下一批 |
| 2026-09-14 | 完成桌面端、Pandaa iPhone、iPad 模拟器及 daemon 开发同步；界面／安装启动与 daemon 回读分别取证，完整业务验收缺口保留 |
| 2026-09-14 | 用户授权提交推送 main；已有 5 个提交保持原样，第三／四批新增 6 个代码提交，本页另作维护文档提交；全量合并门禁及逐组内容检查通过 |

## 8. 下次恢复工作

1. 从本文及本地阶段归档入口恢复上下文，确认实际工作区是 `issue-batch4-0913`、分支 `fix/issue-batch4-0913`；重新读取适用 `AGENTS.md`。
2. 对照阶段 SHA-256 清单检查文件及 Git 状态。若有后续改动，先确认归属；不用旧快照覆盖它们，也不把阶段测试报告当作变化后代码的验收。
3. 第三／四批已按依赖完成提交整理，准确映射见第 6 节。继续时先核对远端 main 是否包含本页及这些代码提交，再从最新基线安排剩余验收；原第三批源工作区和阶段归档继续保留。
4. 按本期范围补真实使用验收：#362 双端项目置顶与重配对；#334 系统多任务、键盘与审批状态。#360 搜索／显式导入属阶段二，#352 仍需当前硬件复现，#379 继续停止 Linux 验证。
5. 本次用户已授权 main 提交推送；正式发布、Issue 回写和关单仍是独立交付动作，未随提交自动执行。用户重新指定下一步后继续，原始失败回执和验收限制保留。
