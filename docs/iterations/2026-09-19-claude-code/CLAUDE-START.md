# 给 Claude Code 的开始指令

将下面整段作为修复任务交给 Claude Code；本文件的存在本身不表示已经启动执行。

```text
请实施 docs/iterations/2026-09-19-claude-code/ 下的八项 Issue 修复计划。

先读根目录 AGENTS.md、目录 README.md、VALIDATION.md，以及八份 issue-*.md。
范围固定为：第一批 #384、#387；第二批 #385、#386、#388、#389；第三批 #390、#375。
不做 #352，不做第四批 #381、#342，不从历史迭代文档追加任务。

接手先确认当前分支、HEAD、工作区与暂存区改动、可用系统和测试设备，并回读 Issue 最新评论。
计划基线是 e5ee088f；若代码已变化，先确认哪些问题仍然存在，避免重复实现。
已有未提交的观测／推送／网络诊断工作必须保留，不得整文件覆盖、清理或顺手提交。

按批次交付；高影响故障的取证可以提前。不确定根因的任务先建立可复现证据，按方案分支选择
最小修复。不要用全局升级、猜测配置路径、修改用户原始会话或放宽权限来换取表面通过。
无对应设备时继续能做的实现／fixture 验证，准确记录缺口；不要把 Windows 单测当作 iOS 或
Debian 真机验证，也不要在没有证据时宣称上游缺陷。

每项独立记录：根因、修改范围、实际执行的测试、未执行项、提交与发布需求。
根因或上游能力与文档不同，可据证据修订本目录后继续范围内工作，不必为每个常规技术选择等待用户。
只有用户取舍或缺少关键外部条件且确实阻塞该任务时，准确说明需要什么，并继续其他任务。

#389 使用 issue-389-push-recovery.md 的完整恢复范围，落实旧用户迁移、多配对、ACK、有限重试、
旧 relay 降级及 iOS 生命周期，不把“已发送”当作“服务端已保存”。方案来源草稿仍保留供对照。

遵守 AGENTS.md 的 daemon 单实例和更新脚本规则；发现其他活动会话时列出，不得自行 FORCE=1。
#389 的现场保护继续适用：不覆盖用户当前手机 App，不擅自部署／重启生产 relay 或本机 daemon，
不向真实用户发送测试通知；在隔离环境及专用设备验证。把部署、安装与发布列为后续交付动作。
不要自动关闭 Issue、向反馈者发消息、更新 GitHub 标签或发布 Release。

先按 VALIDATION.md 做任务级验证，集成时再做所需模块／全量检查；失败分清基线、环境和补丁原因。
最终给用户一份按八项列出的结果表：已修复、待真机验收、被什么阻塞、是否可发布／可关闭。
```
