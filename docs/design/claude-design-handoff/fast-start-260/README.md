# 新任务创建链路（issue #260）——Fast Start

- 在线设计板（2.0 项目）：https://claude.ai/design/p/eb401868-d618-47f7-b8d4-4641117d566d?file=Fast+Start+Direction+v1.dc.html
- 文件：`FastStartDevice.dc.html`（活组件，像素规格源）、`Fast Start Direction v1.dc.html`（board 壳）
- 生成：2026-08-15/16，Opus 5 Max，「cc-pocket Design System 2.0」项目（主题以 2.0 为准；旧 1.0 项目稿仅作交互参照）

## 机主逐轮定稿（最终形态）

1. **手机项目列表页**：导航栏＝标题＋搜索图标（点开行内展开、自动聚焦；收起即清筛选）；**右下角 ＋ 悬浮圆钮（FAB，56dp、accent、投影＋渐变底衬）是页面唯一的新任务入口**，搜索展开时隐藏；导航栏下不再有入口行，列表满高；「打开任意文件夹」独立行从本页消失。树形/平铺切换挂第一个分组标签右侧（44pt）。
2. **点 FAB → 底部弹窗 composer**：多行输入在上；附件行在下＝「项目」chip（文件夹 glyph＋中截断名）＋「智能体」chip（身份色点＋名）居左、terracotta 发送钮居右；默认预填最近项目＋默认智能体；chip 永远可见、点按换；遮罩点按或把手下滑关闭，**草稿保留**。
3. **发送 → 直接跳入新会话的聊天界面**（首条提示词已发出、流式进行中）；失败回弹窗保草稿。机主确认这是预期语义，实现须用测试钉死。
4. **项目选择器**：最近使用（当前打勾）→ 全部项目（可搜）→ 底部钉「浏览其他文件夹…」（原「打开任意文件夹」能力折进此处）。
5. **智能体选择器**：六家（Claude/Codex/OpenCode/Kimi/ZCode/DeepSeek）身份色点＋名，单选；daemon 不支持的 42% 淡显＋「不可用」小标——可见不隐藏。
6. **桌面空态 composer**：同两枚 chip 内联于附件行（popover 代替 sheet），「模式与模型沿用默认」一行小字。
7. **被否决的形态**（勿回退）：导航栏下常驻入口行/就地展开（太占版面）、底部常驻输入条（选择器叠键盘）。

## 边界（照 issue #260）

不动完整新建弹层与 ConfigureSessionSheet；模式/模型走默认；不做提示词智能路由；默认落点不改。
