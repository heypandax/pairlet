本次更新（1.3.1）

新增
• 逐行改动：改动文件列出 +新增 / -删除 行数，点开即是「改动 | 文件」双视图、代码块可折叠——手机与桌面端（全新双栏 Changes 浏览器）都有
• 运行中的会话一会话一行：同一项目多个会话各自可点，恢复时自动用对的后端（Claude / Codex）
• 新会话默认模型：在设置里选一次即可
• 电脑按真实机器名显示，不再是一串 ID 哈希；自己起的昵称依然优先

修复与优化
• 桌面端：侧栏分隔线可拖拽调宽、可贴边收起、点边条恢复；最近会话行的关闭按钮现在真的可用
• 返回列表立即刷新；「运行中」标记随回合结束即时熄灭
• 斜杠命令补全后光标就位，直接输参数

提示：逐行改动需要电脑端 cc-pocket-daemon 同步更新到 1.3.1——运行 cc-pocket-daemon update（或 brew upgrade / scoop update）。
